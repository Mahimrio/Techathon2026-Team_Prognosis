import { Server as SocketIOServer } from 'socket.io'

// TODO: emit deviceUpdate, usageUpdate, alertTriggered, alertResolved
export const registerSocketHandlers = (_io: SocketIOServer) => {
  console.log('[sockets] placeholder — handlers not yet registered')
}
